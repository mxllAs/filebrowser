# 2026-06-30

## P8 回退：FileListing.vue / ListingItem.vue / listing.css
- **P8 改造导致 list 视图布局完全崩坏**（排序头部纵向堆排、左侧大块空白、内容错位）
- 已用 `git checkout HEAD --` 还原三个文件到改造前状态
- DAISYUI_MIGRATION.md 中 P8 标记为回退，待重新设计
- **教训**：FileListing.vue 的三种视图模式（list/mosaic/gallery）共用 ListingItem 组件 + JS 依赖 `.item` class，不能简单替换模板结构

## P8 修复：mosaic 视图卡片布局混乱
- **根因**：`listing.css` 中 mosaic 规则存在重复/冲突 — 第 146 行设 `div:first-of-type { width: 100% }`，第 182 行又覆盖为 `width: 5em`（旧的水平布局残留）
- **修复**：完全重写 `listing.css`，按 list/mosaic/gallery 三个区块清晰分离；mosaic 卡片统一为 `flex-direction: column; align-items: center`
- **修复**：`columnsResize()` 函数改为只在 `viewMode === 'list'` 时执行，避免给 grid 布局的 mosaic item 设置冲突的内联 width
- format + lint 均通过

## Phase 8 — FileListing.vue 列表视图改造 ✅
- `FileListing.vue` list 视图排序头部：旧`.item.header`结构→Tailwind flex 布局（`flex items-center w-full border-b bg-base-200/50 px-3 py-2`）
- `ListingItem.vue` 重写：list 模式下使用 `flex items-center w-full border-b py-2.5 px-3 hover:bg-base-200/50` + `truncate` 截断
- `listing.css` 从 ~299行 精简到 ~160行：仅保留 mosaic/gallery 卡片样式、选择高亮、多选栏、message
- 新增 `viewModeClass` 计算属性动态切换容器 class（list=flex-col, mosaic=grid grid-cols-...）
- DAISYUI_MIGRATION.md P8 ✅

## Phase 7 — Breadcrumbs.vue DaisyUI 改造 ✅
- `Breadcrumbs.vue` 改为 DaisyUI `breadcrumbs text-xs sticky top-12 z-20 bg-base-100 border-b border-base-300/50 px-3 py-1`
- 模板从 `<span>` 结构改为 `<ul><li>` 语义结构，DaisyUI 自动生成分隔符
- 删除 `base.css` 中旧 `.breadcrumbs` 样式 ~36行
- 更新 `styles.css` 中 `#editor-container .breadcrumbs` 适配新的 ul/li 结构
- 删除 `html[dir="rtl"] .breadcrumbs .chevron` RTL 规则（不再需要手动chevron）
- DAISYUI_MIGRATION.md 检查清单 P7 ✅

## Bug 修复（P5 侧边栏 + PC端布局）
- 修复 `frontend/src/css/base.css`：移除 `body { padding-top: 4em }`，解决 sticky header 导致的顶部双重间距。
- 修复 `frontend/src/css/mobile.css`：将移动端侧边栏从 `left` 动画改为 `transform: translateX(-100%)`，并把选择器提升到 `nav.sidebar-nav`。
- 修复 `frontend/src/components/Sidebar.vue`：
  - overlay 改为 `v-if="isMobile && active"` 条件渲染，PC端完全不生成遮罩 DOM
  - 添加 `isMobile` 计算属性（≤736px）
  - 内层 div 背景从 `bg-base-200/85` 提升到 `/90`
- 修复 `frontend/src/css/base.css` nav 样式：从 `top: 4em; height: calc(100vh - 4em)` 改为 `top: 0; height: 100vh; padding-top: 4rem`
- 减少 main 的 padding-top 和 .dashboard 的 margin、.breadcrumbs 的 margin-bottom
- 在 `frontend/DAISYUI_MIGRATION.md` 检查清单下追加 bug 修复记录。

## Phase 9-10 — 进度条 + 收尾 ✅
- 删除 `ProgressBar.vue`：组件在项目中完全未被引用，Layout.vue的进度条用独立CSS
- 删除 `mdPreview.css` (112行)：`.md_preview` 类无任何Vue模板引用，Editor.vue已用 `prose`
- 移除 `styles.css` 中 `mdPreview.css` 的 `@import`

## DaisyUI 迁移全部完成统计
| 文件 | 改前 | 改后 | 缩减 |
|------|------|------|------|
| base.css | ~280行 | ~125行 | -155行 |
| login.css | ~248行 | ~58行 | -190行 |
| listing.css | ~299行 | ~160行 | -139行 |
| mdPreview.css | ~112行 | 删除 | -112行 |
| ProgressBar.vue | ~226行 | 删除 | -226行 |
| **合计** | ~1165行 | ~343行 | **-822行 (70%↓)** |

DAISYUI_MIGRATION.md 检查清单 P1-P10 全部 ✅


